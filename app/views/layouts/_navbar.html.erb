<nav class="bg-white right-4 z-20 top-0 start-0 w-full">
  <div class="flex justify-between items-center w-full mx-auto border-b border-gray-200 dark:border-gray-600">
    <% if current_user %>
        <div class="mb-6 text-xl text-gray-700">
            <p>Logged in as <strong><%= current_user.email %></strong></p>
        </div>
      <div class="ml-auto flex items-center justify-center px-4 py-3 text-base font-medium text-gray-700">
        <%= link_to destroy_user_session_path, data: { turbo_method: :delete }, class: "block" do %>
            <div class="flex items-center px-4 py-2 hover:bg-gray-100">
              <span class="btn btn--negative"">Logout</span>
            </div>
          <% end %>
      </div>
    <% end %>
  </div>
</nav>
