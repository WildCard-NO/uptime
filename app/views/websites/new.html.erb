<% content_for :title, "New website" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New website</h1>

  <%= render "form", website: @website %>

  <%= link_to "Back to websites", websites_path, class: "btn btn--secondary" %>
</div>
